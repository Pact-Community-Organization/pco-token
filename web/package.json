{
  "name": "pco-claim-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.js --bundle --format=iife --outfile=app.js"
  },
  "devDependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0",
    "esbuild": "^0.25.12"
  }
}
